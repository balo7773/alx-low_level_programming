#include <stdio.h>

/**
 * main - prints a string and a new line
 * @argc: datatype int
 * @argv: datatype string
 * Return: Always 0 (success)
 */

int main(int argc, char const *argv[])
{
	(void)argc;

	printf("%s\n", argv[0]);

	return (0);
}
