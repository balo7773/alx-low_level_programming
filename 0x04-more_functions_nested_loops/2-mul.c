#include "main.h"
/**
 * mul - a function that multiplies int a and int b
 * main - Entry point
 * @a: an int variable in the mul function
 * @b: an int variable in the mul function
 * Return: Always 0 (success)
 */
int mul(int a, int b)
{
	int multiply;

	multiply = a * b;
	return (multiply);
}
