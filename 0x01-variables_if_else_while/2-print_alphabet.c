#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int alpbt;

	for (alpbt = 'a'; alpbt <= 'z'; alpbt++)
	{

	putchar(alpbt);

	}
	putchar('\n');

	return (0);

}
