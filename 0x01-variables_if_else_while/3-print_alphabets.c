#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{

	int alpbt;
	int ALPBT;

	for (alpbt = 'a'; alpbt <= 'z'; alpbt++)
	{
		putchar(alpbt);
	}

	for (ALPBT = 'a'; ALPBT <= 'z'; ALPBT++)
	{
		putchar(ALPBT);
	}

	putchar('\n');

	return (0);

}


